<!--components/tab.wxml-->
<view class="tabs">
  <view class="tabs_title">
  <view 
   wx:for="{{tabs}}"
   wx:key="{{item.id}}"
   class="title_item {{item.isActive?'active':''}}"
   catchtap="f0"
   data-index="{{index}}">
    {{item.name}}
  </view>
</view>
<!-- <view class="tabs_content">
  内容
</view> -->
</view>
